fun bool writeln (bool b) =
    let m = write(b) in let n = write("\n") in b

fun bool test () =
    let n = reduce(fn int (int a, int b) => a + b, 1, {0}) in writeln(n == 1)

fun int main () =
    let t = test() in
    0

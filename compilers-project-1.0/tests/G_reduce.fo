fun bool writeln (bool b) =
    let m = write(b) in let n = write("\n") in b

fun bool test () =
    let n = reduce(add, 1000, {300, 30, 7}) in writeln(n == 1337)

fun int main () =
    let t = test() in
    0

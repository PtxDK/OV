fun bool writeln (bool b) =
    let m = write(b) in let n = write("\n") in b

fun bool test03 () =
    if   (true)
    then (writeln(true ))
    else (writeln(false))

fun int main () =
    let t = test() in
    0
